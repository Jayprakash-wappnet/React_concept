//deconstructing in functional components
import React from "react";

const Destructuring = ({Emp_name,address}) => { //as an argument props passed now you can use 
    return <h1>The Emp name is {Emp_name} from {address}</h1>
}

export default Destructuring